{
    "tasks": [
        {
            "type": "shell",
            "label": "build test",
            "command": "python3 '${workspaceFolder}/build.py'",
            "problemMatcher": "$gcc",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "presentation": {
                "clear": true
            },
        }
    ],
    "version": "2.0.0"
}