{
    "tasks": [
        {
            "type": "shell",
            "label": "build test",
            "command": "python3 '${workspaceFolder}/build.py'",
            "problemMatcher": "$gcc",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build"
        }
    ],
    "version": "2.0.0"
}